import { Layout } from "@/layouts/Layout";
import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <Layout>
      <h1 className="font-jetbrains-mono text-lg text-red-400">hello</h1>
    </Layout>
  );
};

export default Home;
